{
  "name": "rho-staging",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ rho-staging",
    "create-local": "graph create --node http://localhost:8020/ rho-staging",
    "remove-local": "graph remove --node http://localhost:8020/ rho-staging",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rho-staging",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.72.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
